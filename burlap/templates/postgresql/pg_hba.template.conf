# TYPE  DATABASE    USER        CIDR-ADDRESS          METHOD
# Postgres user requires a password.
local   all         postgres                          md5
# Allow any user to connect to any database as any database user.
#local   all         all                               trust
#host    all         all         127.0.0.1/32          trust
# Allow any remote connection to connect if they have a valid password.
#host    all         all         0.0.0.0/0             md5